import { DatabaseID } from '../shared/database/constants';
import { Language } from '../shared/language/constants';

export const TopicPathPrefix = `/topic/v1/${DatabaseID}/${Language}`;
export const WopiPathPrefix = `/v1/${DatabaseID}/${Language}`;
